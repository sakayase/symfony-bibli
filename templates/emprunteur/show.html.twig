{% extends 'base.html.twig' %}

{% block title %}Emprunteur{% endblock %}

{% block body %}
    <h1>Emprunteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emprunteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ emprunteur.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ emprunteur.email }}</td>
            </tr>
            <tr>
                <th>Blacklist</th>
                <td>{{ emprunteur.blacklist ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Carte</th>
                <td>{{ emprunteur.carte ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emprunteur_index') }}">back to list</a>

    <a href="{{ path('emprunteur_edit', {'id': emprunteur.id}) }}">edit</a>

    {{ include('emprunteur/_delete_form.html.twig') }}
{% endblock %}
